---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: deployobjects.app.my.domain
spec:
  group: app.my.domain
  names:
    kind: DeployObject
    listKind: DeployObjectList
    plural: deployobjects
    singular: deployobject
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DeployObject is the Schema for the deployobjects API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeployObjectSpec defines the desired state of DeployObject
            properties:
              env:
                description: EnvVar represents an environment variable present in
                  a Container.
                items: {}
                type: array
              image:
                description: Image is container image address
                minLength: 0
                type: string
              ports:
                description: ServicePort contains information on service's port.
                items: {}
                type: array
              replicas:
                description: Replicas is pod replica num
                format: int32
                type: integer
              resources:
                description: Resources describes the compute resource requirements
            required:
            - image
            - ports
            - replicas
            type: object
          status:
            description: DeployObjectStatus defines the observed state of DeployObject
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
